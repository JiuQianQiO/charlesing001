<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhi.boot.mapper.StudentMapper">
    <!--    public Account getAcct(Long id); -->
    <select id="getStudent" resultType="com.zhi.boot.bean.Student">
        select * from  student where  id=#{id}
    </select>
    <select id="studentList" resultType="com.zhi.boot.bean.Student">
        select * from  student
    </select>
</mapper>