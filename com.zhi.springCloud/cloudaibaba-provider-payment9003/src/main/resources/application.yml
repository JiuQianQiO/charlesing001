server:
  port: 9003
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # 服务注册中心 Nacos 的地址
management:
  endpoints:
    web:
      exposure:
        include: "*"